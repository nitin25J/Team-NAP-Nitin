// Central place for backend connection settings.
// Point this at your live backend URL once it's deployed.
// Every future fetch() call in the other JS files should use API_BASE, e.g.:
//   fetch(`${API_BASE}/api/incidents`)
const API_BASE = "http://localhost:5000"; // swap for the deployed backend URL later
